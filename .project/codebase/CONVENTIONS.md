# Code Conventions

> Auto-generated by /spec:new-project on 2026-04-02

## Naming Patterns

**Files:**
- PascalCase for classes and scene files: `FlightScene.ts`, `Ship.ts`, `CelestialBody.ts`
- camelCase for pure-function library modules: `physics.ts`, `orbit.ts`, `cameraZoom.ts`, `scaleConfig.ts`, `timewarp.ts`
- Types barrel file: `src/types/index.ts`

**Functions:**
- camelCase, verb-first, descriptive of action or return value: `computeOrbitalElements()`, `checkSOITransition()`, `shipWorldPosition()`, `predictBodyPosition()`
- Boolean-returning functions use `is`/`has`/`should`/`can` prefix: `isLandableBody()`, `shouldAutoDropWarp()`
- Private helper functions within a module use plain camelCase: `buildOrbitFromState()`, `parseCssRgb()`

**Variables:**
- camelCase for all variables: `warpIndex`, `bodyRadius`, `screenHeight`
- Boolean variables use `is`/`has`/`should` prefix: `isThrusting`, `isGasGiant`, `soiChanged`
- Descriptive, no abbreviations except well-known physics terms: `nu` (true anomaly), `mu` (gravitational parameter), `vx`/`vy` (velocity components), `a`/`e` (semi-major axis, eccentricity)

**Constants:**
- UPPER_SNAKE_CASE for module-level constants: `THRUST_POWER`, `ROTATION_SPEED`, `INITIAL_FUEL`, `MAX_GRADIENT_RADIUS`
- Group related constants into `Readonly<Record<string, T>>` objects when they form a lookup table: `STAR_RADII`, `PLANET_MASSES`
- Use `as const` for frozen config objects: `ZOOM_CONFIG`

**Types and Interfaces:**
- PascalCase for all types: `Vec2`, `OrbitalElements`, `ShipState`, `TrajectorySegment`
- Interfaces for data shapes with named fields: `interface ScreenBounds { minX: number; ... }`
- Type aliases for unions and computed types: `type CelestialBody = StarBody | PlanetBody | MoonBody | AnomalyBody`
- Discriminated unions use a `kind` field: `CelestialBodyKind = 'star' | 'planet' | 'moon' | 'anomaly'`
- String literal union types for state machines: `type LandingState = 'inactive' | 'approach' | 'landed' | 'crashed' | ...`

## Code Style

**Formatting:**
- No dedicated formatter configured (no Prettier, Biome, or EditorConfig detected)
- Consistent 2-space indentation throughout the codebase
- Trailing commas in multi-line parameter lists and arrays
- Single quotes for string literals
- Semicolons required at end of statements

**Linting:**
- No ESLint or other linter configured
- TypeScript strict mode serves as the primary quality gate via `tsconfig.json`:
  - `"strict": true`
  - `"noImplicitAny": true`
  - `"strictNullChecks": true`

**TypeScript Strictness:**
- Use non-null assertion (`!`) sparingly, only for Phaser APIs that return nullable types but are guaranteed non-null in context (e.g., `this.input.keyboard!`)
- Explicit return types on all public/exported functions
- Void return types declared explicitly: `create(): void`

## Import Organization

**Order:**
1. Framework imports (`phaser`, `Phaser`)
2. Type-only imports from project types (`import type { Vec2, ShipState } from '../types/index.js'`)
3. Library function imports from `src/lib/` modules (`import { computeOrbitalElements } from '../lib/orbit.js'`)
4. Sibling/local imports (`import { Ship } from '../objects/Ship'`)

**Style:**
- Use `import type` for type-only imports — never import types with value imports
- Named imports always (no default exports except `vite.config.ts`)
- Destructured imports: `import { Game, AUTO } from 'phaser'`

**Path Format:**
- Relative paths with `../` navigation (no path aliases configured)
- Some imports use `.js` extension suffix (`'../lib/orbit.js'`), others omit it — the bundler resolves both. New code should include `.js` extensions for consistency with newer modules.

## Error Handling

**Patterns:**
- No try/catch blocks in the codebase; errors propagate naturally
- Guard clauses with early returns for invalid state: `if (mag === 0) return { x: 0, y: 0 };`
- Fallback values for parse failures: `if (!match) return 0xffffff;` in `parseCssRgb()`
- `void` keyword to suppress unused variable warnings: `void h;` when a computed value is kept for documentation purposes
- CLAUDE.md mandate: "No silent failures — invalid state should be explicit"

## Logging

**Framework:** None. No console.log, console.warn, or logging framework detected in source.

**Pattern:** The codebase is silent by default. Avoid adding console output unless debugging.

## Comments

**When to Comment:**
- Comments explain "why", not "what" — per CLAUDE.md: code should be self-documenting
- Physics derivations and design rationale get detailed block comments explaining the math or reasoning:
  ```typescript
  // heading 0 = pointing up (-Y), so thrust direction is (-sin, -cos) in world space
  // Phaser rotation: 0 = right, but we track heading independently
  ```
- Scale/tuning constants get multi-line comments with derivation formulas and design targets:
  ```typescript
  // KSP-SCALE TUNING (Feature 0006, Phase 5):
  // At KSP scale orbital velocities are 60-200 gu/s...
  ```
- Intentional no-ops get a comment: `// S is intentionally a no-op`

**Module-Level Comments:**
- Library modules open with a comment block describing purpose and scope:
  ```typescript
  // Landing physics and state machine — pure, framework-agnostic logic.
  // No Phaser imports. All functions are stateless and testable in isolation.
  ```
- Config modules open with a scale regime overview documenting the numeric ranges and physics basis

**JSDoc:**
- Used sparingly, only for complex public functions that need parameter documentation
- Most functions rely on descriptive names and TypeScript types instead of JSDoc

## Function Design

**Size:** Functions are short and focused. The CLAUDE.md 200-line file limit applies at the module level; individual functions are typically 5-30 lines.

**Parameters:**
- Use typed object parameters for functions with 3+ related parameters: `TrajectoryOptions`, `InputState`
- Physics functions take primitives for mathematical clarity: `computeOrbitalElements(pos: Vec2, vel: Vec2, mu: number)`
- `delta` (time step in ms) is passed explicitly, never read from global state

**Return Values:**
- Return plain objects, not classes, from pure functions: `{ pos: Vec2; vel: Vec2 }`
- Null for "not found" / "no transition": `checkSOITransition() → SOITransitionResult | null`
- Boolean for predicate functions: `shouldAutoDropWarp() → boolean`

**Pure vs. Stateful:**
- Prefer pure functions in `src/lib/` — no framework imports, no side effects, testable in isolation
- Stateful classes reserved for `src/objects/` (game entities) and `src/scenes/` (Phaser scenes)
- Module-level mutable state is minimized; `src/lib/timewarp.ts` is a rare exception using a module-scoped `warpIndex`

## Module Design

**Exports:**
- Named exports only (no default exports in source code)
- Export functions, types, interfaces, and constants individually
- Re-export types from barrel files for convenience: `export type { LandingState } from '../lib/landing.js'` in `src/types/index.ts`

**Barrel Files:**
- `src/types/index.ts` is the single barrel file for all shared type definitions
- Library modules (`src/lib/*.ts`) are imported individually, not through a barrel

**Module Boundaries:**
- `src/lib/` modules must NOT import from Phaser — they are framework-agnostic pure logic
- `src/objects/` modules may import from Phaser and from `src/lib/`
- `src/scenes/` modules may import from Phaser, `src/lib/`, and `src/objects/`
- `src/types/` contains only type definitions, no runtime code

**Constants Organization:**
- Game-wide physics/scale constants go in `src/lib/scaleConfig.ts`
- Unit system constants go in `src/lib/units.ts`
- Feature-local constants are declared at the top of their module with UPPER_SNAKE_CASE names
- Scene-specific HUD/rendering constants are declared at the top of the scene file

## Testing Conventions

**Test Framework:** Not yet configured. No test runner, test files, or test dependencies detected.

**CLAUDE.md Mandate:** "Write tests to prove things are working as expected" and "Prefer testable pure functions over stateful classes where possible."

**Testability Architecture:**
- Pure functions in `src/lib/` are designed for testing: no Phaser dependencies, explicit parameters, deterministic outputs
- `src/lib/landing.ts` header explicitly states: "All functions are stateless and testable in isolation"
- When adding tests, co-locate test files next to source or use a top-level `tests/` directory (no existing convention to follow)

**When Tests Are Added:**
- Use a framework compatible with the Vite/TypeScript toolchain (e.g., Vitest)
- Test pure `src/lib/` functions first — they have no framework dependencies
- Focus on physics correctness: orbital mechanics, SOI transitions, landing state machines

---

*Convention analysis: 2026-04-02*
