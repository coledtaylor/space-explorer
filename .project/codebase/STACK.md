# Technology Stack

> Auto-generated by /spec:new-project on 2026-04-02

## Languages

**Primary:**
- TypeScript 5.9.3 — All application code in `src/`

**Secondary:**
- HTML — Single entry point `index.html`
- CSS — Inline styles only (in `index.html`)

## Runtime

**Environment:**
- Browser (client-side only, no server component)
- ES2020 target (configured in `tsconfig.json`)

**Package Manager:**
- npm
- Lockfile: `package-lock.json` (present)

## Frameworks

**Core:**
- Phaser 4.0.0-rc.7 — Game engine (2D rendering, input, scene management)

**Testing:**
- Not detected — No test framework configured

**Build/Dev:**
- Vite 5.4.21 — Dev server and production bundler (`vite.config.ts`)
- TypeScript 5.9.3 — Type checking (strict mode enabled)

## Key Dependencies

| Dependency | Version | Purpose |
|------------|---------|---------|
| phaser | 4.0.0-rc.7 | Core game engine — scenes, rendering, input, physics |
| typescript | 5.9.3 | Static type checking (devDependency) |
| vite | 5.4.21 | Dev server with HMR and production bundler (devDependency) |

This is a minimal dependency project. Phaser is the only runtime dependency.

## Configuration

**TypeScript (`tsconfig.json`):**
- Target: ES2020
- Module: ESNext with bundler resolution
- Strict mode: enabled (`strict`, `noImplicitAny`, `strictNullChecks`)
- Source scope: `src/` directory only

**Vite (`vite.config.ts`):**
- Default configuration (no custom plugins or settings)

**Build Scripts (`package.json`):**
- `npm run dev` — Start Vite dev server
- `npm run build` — Production build to `dist/`
- `npm run preview` — Preview production build

## Database & Storage

**Databases:**
- None — Client-side browser game with no persistence

**File Storage:**
- Local filesystem only (no asset loading detected yet; ship is drawn procedurally via Phaser Graphics)

**Caching:**
- None

## External Integrations

**APIs & Services:**
- None — Fully offline client-side application

## Platform Requirements

**Development:**
- Node.js (for npm and Vite dev server)
- npm (package manager)
- Modern browser with ES2020 support

**Production:**
- Static file hosting only — `dist/` contains the built output
- No server-side runtime required

**Browser Compatibility:**
- Any browser supporting ES2020 (Chrome 80+, Firefox 80+, Safari 14+, Edge 80+)

## Notes

- Phaser 4 is a release candidate (rc.7), not a stable release. The `^4.0.0-rc.7` range in `package.json` may pull newer RCs.
- No linting (ESLint), formatting (Prettier), or testing frameworks are configured.
- No path aliases are configured in `tsconfig.json` — all imports use relative paths.
- The project uses ES modules (`"type": "module"` in `package.json`).

---

*Stack analysis: 2026-04-02*
