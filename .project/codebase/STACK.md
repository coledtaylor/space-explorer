# Technology Stack

> Auto-generated by /spec:new-project on 2026-04-01

## Languages

**Primary:**
- JavaScript (ES2020+) — All game logic, rendering, and UI (`js/*.js`)

**Secondary:**
- HTML5 — Single entry point (`index.html`)
- CSS3 — UI styling with modern features like `backdrop-filter`, `inset`, CSS custom properties (`css/style.css`)

## Runtime

**Environment:**
- Web browser (any modern browser supporting ES modules and Canvas 2D)
- No server-side runtime required

**Package Manager:**
- None — zero dependencies, no `package.json`, no `node_modules`
- Lockfile: Not applicable

## Frameworks

**Core:**
- None — vanilla JavaScript with native browser APIs only

**Testing:**
- None — no test framework configured

**Build/Dev:**
- None — no build step, no bundler, no transpiler
- Serve `index.html` over HTTP for ES module support (e.g., `npx serve`, `python -m http.server`, VS Code Live Server)

## Key Dependencies

| Dependency | Version | Purpose |
|------------|---------|---------|
| (none) | — | Zero external dependencies. All code is hand-written. |

**Browser APIs used:**
- Canvas 2D API — all rendering (`js/renderer.js`, `js/ship.js`, `js/starfield.js`)
- `requestAnimationFrame` — game loop (`js/main.js`)
- DOM API — HUD and UI panel manipulation (`js/main.js`)
- Keyboard/Mouse events — player input (`js/input.js`)
- ES Modules (`import`/`export`) — module system (`index.html` loads `js/main.js` as `type="module"`)

## Configuration

**Environment:**
- No environment variables
- No configuration files
- No `.env` or runtime config needed

**Build:**
- No build configuration — static files served directly
- `.gitignore` excludes `node_modules/`, `.DS_Store`, `Thumbs.db`, `*.log`

## Database & Storage

**Databases:**
- None — all state is in-memory during gameplay

**File Storage:**
- None — no persistence layer

**Caching:**
- None — no caching strategy

## External Integrations

**APIs & Services:**
- None — fully self-contained, offline-capable browser game

## Platform Requirements

**Development:**
- Any HTTP server capable of serving static files (ES modules require HTTP, not `file://`)
- No Node.js, npm, or build tools required
- Any text editor

**Production:**
- Any static file host (GitHub Pages, Netlify, S3, any web server)
- Files to deploy: `index.html`, `css/style.css`, `js/*.js` (6 JS files)
- No server-side processing needed

**Browser Compatibility:**
- Requires ES module support (`<script type="module">`)
- Requires Canvas 2D API
- Requires `backdrop-filter` CSS for full visual fidelity (graceful degradation without it)
- Target: modern evergreen browsers (Chrome, Firefox, Safari, Edge)

---

*Stack analysis: 2026-04-01*
