# Project Structure

> Auto-generated by /spec:new-project on 2026-04-01

## Directory Layout

```
space-explorer/
├── css/                # Stylesheets (HUD, overlays, layout)
│   └── style.css       # Single stylesheet for all UI
├── js/                 # All game JavaScript modules (ES modules)
│   ├── main.js         # Entry point — game loop, UI wiring, state
│   ├── celestial.js    # Procedural star system generation and orbits
│   ├── input.js        # Keyboard and mouse input handling
│   ├── renderer.js     # Canvas 2D rendering for celestial bodies and minimap
│   ├── ship.js         # Ship class — physics, trail, drawing
│   ├── starfield.js    # Parallax background with twinkling stars
│   └── utils.js        # Pure math helpers (lerp, dist, clamp, seededRandom)
├── .claude/            # Claude Code configuration (agents, commands, skills)
├── .project/           # Spec project metadata and codebase analysis
├── index.html          # Single HTML entry point — loads game canvas and UI overlay
├── CLAUDE.md           # Project instructions for Claude Code
├── README.md           # Project readme
└── .gitignore          # Ignores node_modules, OS files, logs
```

## Directory Purposes

**`js/`:**
- Purpose: All game source code as ES modules
- Contains: 7 JavaScript files, each responsible for one concern
- Key files: `js/main.js` (entry point and game loop), `js/utils.js` (shared helpers)

**`css/`:**
- Purpose: All visual styling for the HTML UI layer
- Contains: Single stylesheet `css/style.css` (207 lines)
- Key files: `css/style.css` — styles HUD, start screen, scan panel, minimap, canvas

**`.claude/`:**
- Purpose: Claude Code tooling configuration (agents, commands, skills, hooks)
- Contains: Agent definitions, slash commands, skill definitions
- Not part of the game source code

**`.project/`:**
- Purpose: Spec project metadata and codebase analysis documents
- Contains: Auto-generated analysis files
- Generated: Yes
- Committed: No (or managed by spec tooling)

## Key File Locations

**Entry Points:**
- `index.html`: Single HTML page — loads `css/style.css` and `js/main.js` (as ES module)
- `js/main.js`: Game bootstrap — initializes canvas, game loop (`requestAnimationFrame`), all UI event listeners

**Configuration:**
- `.gitignore`: Ignores `node_modules/`, `.DS_Store`, `Thumbs.db`, `*.log`
- `CLAUDE.md`: Project conventions and architecture guidance

**Core Logic:**
- `js/main.js` (196 lines): Game loop, camera, state management, UI wiring
- `js/celestial.js` (137 lines): `generateSystem(seed)` and `updateOrbits()` — procedural generation
- `js/renderer.js` (158 lines): `drawBody()`, `drawMinimap()`, `setCameraHack()` — all canvas rendering
- `js/ship.js` (125 lines): `Ship` class — thrust, drag, fuel, trail, drawing

**Supporting Modules:**
- `js/input.js` (59 lines): `Input` class — WASD/arrow keys, E, M, mouse tracking
- `js/starfield.js` (47 lines): `Starfield` class — 3-layer parallax background
- `js/utils.js` (45 lines): Pure functions — `lerp`, `dist`, `clamp`, `seededRandom`, `hslToRgb`

**Styling:**
- `css/style.css` (207 lines): Full UI styling — HUD overlay, start screen, scan panel, minimap positioning

**Testing:**
- No test files exist. No test framework is configured.

## Naming Conventions

**Files:**
- All lowercase, single-word names: `ship.js`, `input.js`, `utils.js`
- One module per concern — named after the concept it owns
- No prefixes, suffixes, or dots in filenames (no `ship.module.js` or `Ship.js`)

**Directories:**
- All lowercase, short names: `js/`, `css/`
- Flat structure — no nesting within `js/` or `css/`

## Where to Add New Code

**New Game Feature (e.g., inventory, combat, trading):**
- Create a new module: `js/<feature-name>.js`
- Export functions or a class from the new module
- Import and wire into `js/main.js` (which owns the game loop and state)
- Add any UI elements to `index.html` and style in `css/style.css`

**New Celestial Body Type or Generation Logic:**
- Add to `js/celestial.js` (generation) and `js/renderer.js` (drawing)

**New Input Bindings:**
- Add key handling to `js/input.js`
- Wire the action into `js/main.js`

**New Utility Functions:**
- Add to `js/utils.js` — keep them pure and stateless

**New UI Panel or HUD Element:**
- Add HTML markup to `index.html` inside `#ui-overlay`
- Style in `css/style.css`
- Wire DOM interaction in `js/main.js`

**Assets (images, audio, fonts):**
- No assets directory exists yet. Create `assets/` at root if needed (e.g., `assets/images/`, `assets/audio/`)

**Tests:**
- No test infrastructure exists. If adding tests, create a `tests/` directory at root with files named `<module>.test.js`

## Special Directories

**`.claude/`:**
- Purpose: Claude Code agent tooling (not game code)
- Generated: Partially (managed by spec tooling)
- Committed: Yes

**`.project/`:**
- Purpose: Codebase analysis documents for spec tooling
- Generated: Yes
- Committed: Varies by project

**`node_modules/`:**
- Purpose: Would hold npm dependencies if any were added
- Generated: Yes
- Committed: No (in `.gitignore`)
- Note: Currently does not exist — project has zero npm dependencies

---

*Structure analysis: 2026-04-01*
